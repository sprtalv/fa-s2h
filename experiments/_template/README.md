# Experiment Template

## Purpose

TODO(fas2h): describe the experiment goal in 1-3 sentences.

## Config

- Config source: `configs/...`
- Seed: `TBD`
- Data version: `TBD`

## Key Changes

- TODO(fas2h): note code, config, or environment differences from prior runs.

## Run Notes

- Command: see `command.sh`
- Commit: see `git_commit.txt`
- Logs: see `logs/`

## Current Conclusion

- TODO(fas2h): write the current takeaway, even if the result is negative.

## Repro Checklist

- Confirm the copied `config.yaml` matches the executed run.
- Confirm `git_commit.txt` matches the code revision.
- Confirm `data_version.txt` describes the exact dataset snapshot.
- Confirm dependencies are recorded if they differ from the repo default.
